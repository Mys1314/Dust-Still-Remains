using UnityEngine;

public class HoldPose : MonoBehaviour
{
    [Tooltip("A child transform inside the prefab that defines how this item should be oriented when held.")]
    public Transform holdAnchor;
}
